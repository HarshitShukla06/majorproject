[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback - routes all requests to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Build settings
[build.environment]
  NODE_VERSION = "18"
